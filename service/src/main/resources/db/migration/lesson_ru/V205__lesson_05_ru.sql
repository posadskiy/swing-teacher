-- Flyway migration V3: Lesson 5
-- This migration contains lesson, tasks, and documentation for lesson 5

-- ============================================================================
-- TASKS AND DOCUMENTATION
-- ============================================================================

INSERT INTO documentation_translation (documentation_id, language_code, text)
VALUES (49, 'ru', '<div class="lesson-header">
<h2>🎮 Урок 1: Создание текстовой области</h2>
<p class="intro">Текстовые области (<code>JTextArea</code>) нужны для длинных текстов. Комментарии, описания, заметки, сообщения — всё это требует многострочного ввода. В отличие от <code>JTextField</code>, здесь можно писать в несколько строк.</p>
</div>

<section class="learning-objectives">
<h3>📚 Изучаем:</h3>
<ul>
  <li>Класс <code>JTextArea</code> — многострочное поле для ввода текста</li>
  <li>Создание текстовой области</li>
  <li>Разница между однострочными и многострочными полями</li>
</ul>
</section>

<section class="importance">
<h3>💎 Зачем это нужно:</h3>
<p>Текстовые области нужны для длинных текстов. Без текстовых областей пользователь не сможет ввести длинный текст.</p>
</section>

<section class="implementation">
<h3>⚔️ Реализация:</h3>
<ol>
  <li>Создай текстовую область: <code>JTextArea area = new JTextArea();</code></li>
  <li>Добавь на форму: <code>add(area);</code></li>
</ol>
</section>

<section class="best-practices">
<h3>💡 Рекомендации:</h3>
<ul>
  <li><strong>✅ Хорошо:</strong> Создать с размерами: <code>new JTextArea(5, 20)</code> — 5 строк, 20 символов</li>
  <li><strong>✅ Отлично:</strong> Создать с начальным текстом: <code>new JTextArea("Текст")</code></li>
  <li><strong>⚠️ Приемлемо:</strong> <code>add(new JTextArea());</code> — потом не настроишь</li>
</ul>
<p><strong>Важно:</strong> Текстовая область может содержать много строк текста. Можно задать размеры в конструкторе: <code>new JTextArea(строки, столбцы)</code>. Обычно используется вместе с <code>JScrollPane</code> для прокрутки. Текст переносится автоматически (если включён wrap). Используй для комментариев, описаний, длинных текстов.</p>
</section>

<section class="result">
<h3>👀 Результат:</h3>
<p>На форме появится многострочное текстовое поле. Это большая прямоугольная область, куда можно вводить текст в несколько строк. Пользователь может кликнуть и начать печатать.</p>
</section>')
ON CONFLICT (documentation_id, language_code) DO NOTHING;

INSERT INTO task_translation (task_id, language_code, title, question)
VALUES (49, 'ru', 'Текстовая область-новичок: создай многострочное поле',
        'Создай текстовую область и добавь её на форму')
ON CONFLICT (task_id, language_code) DO NOTHING;
INSERT INTO documentation_translation (documentation_id, language_code, text)
VALUES (50, 'ru', '<div class="lesson-header">
<h2>🎮 Урок 2: Перенос строк</h2>
<p class="intro">Перенос строк делает текст читаемым. Без переноса длинная строка уйдёт за границу области и её нужно будет прокручивать горизонтально. С переносом текст автоматически переносится и читается удобнее.</p>
</div>

<section class="learning-objectives">
<h3>📚 Изучаем:</h3>
<ul>
  <li>Метод <code>setLineWrap(boolean wrap)</code> — включение переноса строк</li>
  <li>Метод <code>setWrapStyleWord(boolean wrap)</code> — перенос по словам</li>
  <li>Автоматический перенос длинных строк</li>
</ul>
</section>

<section class="importance">
<h3>💎 Зачем это нужно:</h3>
<p>Перенос строк делает текст читаемым без горизонтальной прокрутки. Это улучшает пользовательский опыт при работе с длинными текстами.</p>
</section>

<section class="implementation">
<h3>⚔️ Реализация:</h3>
<ol>
  <li>Создай текстовую область: <code>JTextArea area = new JTextArea();</code></li>
  <li>Включи перенос строк: <code>area.setLineWrap(true);</code><br><code>area.setWrapStyleWord(true);</code></li>
  <li>Добавь на форму: <code>add(area);</code></li>
</ol>
</section>

<section class="best-practices">
<h3>💡 Рекомендации:</h3>
<ul>
  <li><strong>✅ Хорошо:</strong> <code>setLineWrap(true)</code> + <code>setWrapStyleWord(true)</code> — перенос по словам</li>
  <li><strong>✅ Отлично:</strong> Перенос по словам — слова не разрываются</li>
  <li><strong>⚠️ Избегай:</strong> Только <code>setLineWrap(true)</code> — слова могут разрываться</li>
  <li><strong>❌ Плохо:</strong> Без переноса — длинные строки неудобны</li>
</ul>
<p><strong>Важно:</strong> Обычно используют оба метода вместе. Перенос делает текст читаемым без горизонтальной прокрутки. Рекомендуется для всех текстовых областей.</p>
</section>

<section class="result">
<h3>👀 Результат:</h3>
<p>Длинные строки будут автоматически переноситься. Если текст не влезает в ширину области, он перенесётся на новую строку. Слова не будут разрываться (если включён <code>setWrapStyleWord(true)</code>).</p>
</section>')
ON CONFLICT (documentation_id, language_code) DO NOTHING;

INSERT INTO task_translation (task_id, language_code, title, question)
VALUES (50, 'ru', 'Перенос строк: включи автоматический перенос',
        'Создай текстовую область, включи перенос строк по словам и добавь на форму')
ON CONFLICT (task_id, language_code) DO NOTHING;
