-- Flyway migration V3: Lesson 11
-- This migration contains lesson, tasks, and documentation for lesson 11

-- ============================================================================
-- TASKS AND DOCUMENTATION
-- ============================================================================

INSERT INTO documentation_translation (documentation_id, language_code, text)
VALUES (94, 'ru', '<div class="lesson-header">
<h2>🎮 Урок 1: Создание списка элементов</h2>
<p class="intro">Списки (<code>JList</code>) отображают множество элементов. Файлы, товары, пользователи — всё это удобно показывать в списке. Списки делают данные организованными и доступными для выбора.</p>
</div>

<section class="learning-objectives">
<h3>📚 Изучаем:</h3>
<ul>
  <li>Класс <code>JList</code> — список элементов</li>
  <li>Создание списка с данными</li>
  <li>Отображение элементов в списке</li>
</ul>
</section>

<section class="importance">
<h3>💎 Зачем это нужно:</h3>
<p>Списки позволяют организованно отображать множество элементов и обеспечивают удобный выбор.</p>
</section>

<section class="implementation">
<h3>⚔️ Реализация:</h3>
<ol>
  <li>Создай массив элементов: <code>String[] items = {"Item 1", "Item 2", "Item 3"};</code></li>
  <li>Создай список: <code>JList&lt;String&gt; list = new JList&lt;&gt;(items);</code></li>
  <li>Добавь на форму: <code>add(list);</code></li>
</ol>
</section>

<section class="best-practices">
<h3>💡 Рекомендации:</h3>
<ul>
  <li><strong>✅ Хорошо:</strong> Создать с массивом элементов</li>
  <li><strong>✅ Отлично:</strong> Использовать типизированный <code>JList&lt;String&gt;</code>, использовать DefaultListModel для динамического списка</li>
  <li><strong>⚠️ Избегай:</strong> Пустого списка — пользователь не увидит элементы</li>
</ul>
<p><strong>Важно:</strong> Можно создать с массивом: <code>new JList&lt;&gt;(items)</code>. Можно создать с моделью: <code>new JList&lt;&gt;(model)</code>. Получить выбранный: <code>String selected = list.getSelectedValue();</code>. Установить выбранный: <code>list.setSelectedIndex(0);</code>. Списки лучше помещать в JScrollPane для прокрутки.</p>
</section>

<section class="result">
<h3>👀 Результат:</h3>
<p>На форме появится список элементов. Это вертикальный список с элементами. Можно выбрать элемент кликом.</p>
</section>')
ON CONFLICT (documentation_id, language_code) DO NOTHING;

INSERT INTO task_translation (task_id, language_code, title, question)
VALUES (94, 'ru', 'Список-новичок: создай список элементов',
        'Создай список с элементами "Элемент 1", "Элемент 2", "Элемент 3" и добавь на форму')
ON CONFLICT (task_id, language_code) DO NOTHING;

INSERT INTO documentation_translation (documentation_id, language_code, text)
VALUES (95, 'ru', '<div class="lesson-header">
<h2>🎮 Урок 2: Режим выбора в списке</h2>
<p class="intro">Режим выбора определяет поведение списка. Один файл для открытия, несколько файлов для удаления — разные задачи требуют разных режимов. Правильный режим делает интерфейс интуитивным.</p>
</div>

<section class="learning-objectives">
<h3>📚 Изучаем:</h3>
<ul>
  <li>Метод <code>setSelectionMode(int mode)</code> — установка режима выбора</li>
  <li>Режимы: <code>SINGLE_SELECTION</code>, <code>SINGLE_INTERVAL_SELECTION</code>, <code>MULTIPLE_INTERVAL_SELECTION</code></li>
  <li>Управление выбором элементов в списке</li>
</ul>
</section>

<section class="importance">
<h3>💎 Зачем это нужно:</h3>
<p>Правильный режим выбора делает интерфейс интуитивным и соответствует ожиданиям пользователя.</p>
</section>

<section class="implementation">
<h3>⚔️ Реализация:</h3>
<ol>
  <li>Создай список: <code>JList&lt;String&gt; list = new JList&lt;&gt;(items);</code></li>
  <li>Установи режим выбора: <code>list.setSelectionMode(ListSelectionModel.SINGLE_SELECTION);</code></li>
  <li>Добавь на форму: <code>add(list);</code></li>
</ol>
</section>

<section class="best-practices">
<h3>💡 Рекомендации:</h3>
<ul>
  <li><strong>✅ Используй:</strong> <code>SINGLE_SELECTION</code> для выбора одного элемента</li>
  <li><strong>✅ Отлично:</strong> <code>MULTIPLE_INTERVAL_SELECTION</code> для множественного выбора, <code>SINGLE_INTERVAL_SELECTION</code> для выбора диапазона</li>
  <li><strong>⚠️ Избегай:</strong> Множественного выбора там, где нужен один — путает пользователя</li>
</ul>
<p><strong>Важно:</strong> SINGLE_INTERVAL_SELECTION — выбрать один диапазон (Shift+клик). MULTIPLE_INTERVAL_SELECTION — выбрать несколько элементов (Ctrl+клик). Используй SINGLE_SELECTION для простых случаев.</p>
</section>

<section class="result">
<h3>👀 Результат:</h3>
<p>Список будет работать в выбранном режиме. Можно выбрать один элемент, диапазон или несколько элементов.</p>
</section>')
ON CONFLICT (documentation_id, language_code) DO NOTHING;

INSERT INTO task_translation (task_id, language_code, title, question)
VALUES (95, 'ru', 'Режим выбора: один или несколько элементов',
        'Создай список, установи режим MULTIPLE_INTERVAL_SELECTION через setSelectionMode() и добавь на форму')
ON CONFLICT (task_id, language_code) DO NOTHING;

INSERT INTO documentation_translation (documentation_id, language_code, text)
VALUES (96, 'ru', '<div class="lesson-header">
<h2>🎮 Урок 3: Получение выбранного элемента</h2>
<p class="intro">Получение выбранного элемента позволяет обработать выбор пользователя. Можно выполнить действие с выбранным элементом, обновить другие компоненты. Без этого список будет только для отображения.</p>
</div>

<section class="learning-objectives">
<h3>📚 Изучаем:</h3>
<ul>
  <li>Метод <code>getSelectedValue()</code> — получение выбранного элемента</li>
  <li>Метод <code>getSelectedIndex()</code> — получение индекса выбранного элемента</li>
  <li>Работа с выбранными элементами</li>
</ul>
</section>

<section class="importance">
<h3>💎 Зачем это нужно:</h3>
<p>Получение выбранного элемента необходимо для обработки действий пользователя и обновления интерфейса.</p>
</section>

<section class="implementation">
<h3>⚔️ Реализация:</h3>
<ol>
  <li>Создай список: <code>JList&lt;String&gt; list = new JList&lt;&gt;(items);</code></li>
  <li>Получи выбранный элемент: <code>String selected = list.getSelectedValue();</code></li>
  <li>Используй выбранный элемент: <code>JLabel label = new JLabel("Выбрано: " + selected);<br>
add(label);</code></li>
</ol>
</section>

<section class="best-practices">
<h3>💡 Рекомендации:</h3>
<ul>
  <li><strong>✅ Используй:</strong> <code>getSelectedValue()</code> для получения элемента, <code>getSelectedIndex()</code> для получения индекса</li>
  <li><strong>✅ Отлично:</strong> Проверять на null перед использованием, использовать в обработчике событий</li>
  <li><strong>⚠️ Избегай:</strong> Игнорировать null — может вызвать ошибку</li>
</ul>
<p><strong>Важно:</strong> <code>getSelectedValuesList()</code> — получить список выбранных элементов. Используй в ListSelectionListener.</p>
</section>

<section class="result">
<h3>👀 Результат:</h3>
<p>Можно получить выбранный элемент. При выборе можно узнать, какой элемент выбран.</p>
</section>')
ON CONFLICT (documentation_id, language_code) DO NOTHING;

INSERT INTO task_translation (task_id, language_code, title, question)
VALUES (96, 'ru', 'Выбранный элемент: получи выбранный элемент',
        'Создай список, получи выбранный элемент через getSelectedValue(), выведи его')
ON CONFLICT (task_id, language_code) DO NOTHING;

INSERT INTO documentation_translation (documentation_id, language_code, text)
VALUES (97, 'ru', '<div class="lesson-header">
<h2>🎮 Урок 4: Обработка выбора элементов</h2>
<p class="intro">Обработка выбора делает список интерактивным. Можно реагировать на выбор пользователя, обновлять другие компоненты, выполнять действия. Без обработки выбор не будет иметь эффекта.</p>
</div>

<section class="learning-objectives">
<h3>📚 Изучаем:</h3>
<ul>
  <li>Интерфейс <code>ListSelectionListener</code> — слушатель выбора</li>
  <li>Метод <code>addListSelectionListener()</code> — добавление слушателя</li>
  <li>Обработка событий выбора</li>
</ul>
</section>

<section class="importance">
<h3>💎 Зачем это нужно:</h3>
<p>Обработка выбора позволяет реагировать на действия пользователя и обновлять интерфейс в реальном времени.</p>
</section>

<section class="implementation">
<h3>⚔️ Реализация:</h3>
<ol>
  <li>Создай список: <code>JList&lt;String&gt; list = new JList&lt;&gt;(items);</code></li>
  <li>Добавь слушатель: <code>list.addListSelectionListener(e -> {<br>
&nbsp;&nbsp;if (!e.getValueIsAdjusting()) {<br>
&nbsp;&nbsp;&nbsp;&nbsp;String selected = list.getSelectedValue();<br>
&nbsp;&nbsp;&nbsp;&nbsp;label.setText("Выбрано: " + selected);<br>
&nbsp;&nbsp;}<br>
});</code></li>
  <li>Добавь на форму: <code>add(list);</code></li>
</ol>
</section>

<section class="best-practices">
<h3>💡 Рекомендации:</h3>
<ul>
  <li><strong>✅ Используй:</strong> <code>addListSelectionListener()</code> для обработки</li>
  <li><strong>✅ Отлично:</strong> Проверять <code>getValueIsAdjusting()</code> для избежания множественных событий, обновлять другие компоненты при выборе, использовать лямбда-выражения для краткости</li>
  <li><strong>⚠️ Избегай:</strong> Игнорировать <code>getValueIsAdjusting()</code> — множественные события</li>
</ul>
<p><strong>Важно:</strong> Проверяй <code>getValueIsAdjusting()</code> для избежания лишних событий. Используй для обновления других компонентов.</p>
</section>

<section class="result">
<h3>👀 Результат:</h3>
<p>При выборе элемента будет срабатывать обработчик. Можно обработать выбор и выполнить действия.</p>
</section>')
ON CONFLICT (documentation_id, language_code) DO NOTHING;

INSERT INTO task_translation (task_id, language_code, title, question)
VALUES (97, 'ru', 'Слушатель выбора: обрабатывай выбор элементов',
        'Создай список, добавь ListSelectionListener, проверь getValueIsAdjusting(), получи выбранный элемент и выведи его')
ON CONFLICT (task_id, language_code) DO NOTHING;

INSERT INTO documentation_translation (documentation_id, language_code, text)
VALUES (98, 'ru', '<div class="lesson-header">
<h2>🎮 Урок 5: Модель списка</h2>
<p class="intro">DefaultListModel позволяет динамически управлять элементами. Можно добавлять новые элементы, удалять старые, изменять список во время работы. Без модели список статичен.</p>
</div>

<section class="learning-objectives">
<h3>📚 Изучаем:</h3>
<ul>
  <li>Класс <code>DefaultListModel</code> — модель списка</li>
  <li>Методы <code>addElement()</code>, <code>removeElement()</code>, <code>getElementAt()</code></li>
  <li>Динамическое управление элементами списка</li>
</ul>
</section>

<section class="importance">
<h3>💎 Зачем это нужно:</h3>
<p>DefaultListModel позволяет динамически изменять список во время работы приложения, что необходимо для интерактивных интерфейсов.</p>
</section>

<section class="implementation">
<h3>⚔️ Реализация:</h3>
<ol>
  <li>Создай модель: <code>DefaultListModel&lt;String&gt; model = new DefaultListModel&lt;&gt;();</code></li>
  <li>Добавь элементы: <code>model.addElement("Element 1");</code><br><code>model.addElement("Элемент 2");</code></li>
  <li>Создай список с моделью: <code>JList&lt;String&gt; list = new JList&lt;&gt;(model);</code></li>
</ol>
</section>

<section class="best-practices">
<h3>💡 Рекомендации:</h3>
<ul>
  <li><strong>✅ Используй:</strong> DefaultListModel для динамического списка</li>
  <li><strong>✅ Отлично:</strong> Использовать <code>addElement()</code> для добавления, <code>removeElement()</code> для удаления, <code>getElementAt()</code> для получения элемента</li>
  <li><strong>⚠️ Избегай:</strong> Использовать массив для динамического списка — неудобно</li>
</ul>
<p><strong>Важно:</strong> <code>getSize()</code> — получить размер модели. Используй для списков, которые изменяются во время работы.</p>
</section>

<section class="result">
<h3>👀 Результат:</h3>
<p>Список будет работать с моделью. Можно добавлять и удалять элементы динамически.</p>
</section>')
ON CONFLICT (documentation_id, language_code) DO NOTHING;

INSERT INTO task_translation (task_id, language_code, title, question)
VALUES (98, 'ru', 'Модель списка: работай с DefaultListModel',
        'Создай DefaultListModel, добавь элементы через addElement(), создай список с моделью и добавь на форму')
ON CONFLICT (task_id, language_code) DO NOTHING;

INSERT INTO documentation_translation (documentation_id, language_code, text)
VALUES (99, 'ru', '<div class="lesson-header">
<h2>🎮 Урок 6: Прокручиваемый список</h2>
<p class="intro">Прокрутка позволяет отображать длинные списки. Сотни элементов, тысячи записей — всё это удобно просматривать с прокруткой. Без прокрутки длинный список не поместится на экране.</p>
</div>

<section class="learning-objectives">
<h3>📚 Изучаем:</h3>
<ul>
  <li>Класс <code>JScrollPane</code> — прокручиваемая панель</li>
  <li>Добавление списка в JScrollPane</li>
  <li>Создание прокручиваемого списка</li>
</ul>
</section>

<section class="importance">
<h3>💎 Зачем это нужно:</h3>
<p>Прокрутка необходима для отображения длинных списков и улучшения пользовательского опыта.</p>
</section>

<section class="implementation">
<h3>⚔️ Реализация:</h3>
<ol>
  <li>Создай список: <code>JList&lt;String&gt; list = new JList&lt;&gt;(items);</code></li>
  <li>Создай прокручиваемую панель: <code>JScrollPane scrollPane = new JScrollPane(list);</code></li>
  <li>Добавь на форму: <code>add(scrollPane);</code></li>
</ol>
</section>

<section class="best-practices">
<h3>💡 Рекомендации:</h3>
<ul>
  <li><strong>✅ Используй:</strong> Всегда использовать JScrollPane для списков</li>
  <li><strong>✅ Отлично:</strong> Настраивать размеры прокручиваемой панели, использовать <code>setPreferredSize()</code> для размера</li>
  <li><strong>⚠️ Избегай:</strong> Списка без прокрутки — может не поместиться</li>
</ul>
<p><strong>Важно:</strong> Настраивай размеры для удобства.</p>
</section>

<section class="result">
<h3>👀 Результат:</h3>
<p>Список будет прокручиваемым. Если элементов много, появится скроллбар. Можно прокручивать список.</p>
</section>')
ON CONFLICT (documentation_id, language_code) DO NOTHING;

INSERT INTO task_translation (task_id, language_code, title, question)
VALUES (99, 'ru', 'Прокручиваемый список: добавь прокрутку',
        'Создай список, оберни его в JScrollPane и добавь на форму')
ON CONFLICT (task_id, language_code) DO NOTHING;

INSERT INTO documentation_translation (documentation_id, language_code, text)
VALUES (100, 'ru', '<div class="lesson-header">
<h2>🎮 Урок 7: Множественный выбор</h2>
<p class="intro">Множественный выбор позволяет работать с несколькими элементами. Удалить несколько файлов, выбрать несколько товаров — всё это требует множественного выбора. Без этого придётся выбирать по одному.</p>
</div>

<section class="learning-objectives">
<h3>📚 Изучаем:</h3>
<ul>
  <li>Метод <code>getSelectedValuesList()</code> — получение списка выбранных элементов</li>
  <li>Метод <code>getSelectedIndices()</code> — получение индексов выбранных элементов</li>
  <li>Работа с множественным выбором</li>
</ul>
</section>

<section class="importance">
<h3>💎 Зачем это нужно:</h3>
<p>Множественный выбор необходим для массовых операций и улучшения эффективности работы пользователя.</p>
</section>

<section class="implementation">
<h3>⚔️ Реализация:</h3>
<ol>
  <li>Создай список с множественным выбором: <code>JList&lt;String&gt; list = new JList&lt;&gt;(items);</code><br><code>list.setSelectionMode(ListSelectionModel.MULTIPLE_INTERVAL_SELECTION);</code></li>
  <li>Получи все выбранные элементы: <code>List&lt;String&gt; selected = list.getSelectedValuesList();</code></li>
  <li>Используй выбранные элементы: <code>JLabel label = new JLabel();<br>
for (String item : selected) {<br>
&nbsp;&nbsp;label.setText("Выбрано: " + item);<br>
}<br>
add(label);</code></li>
</ol>
</section>

<section class="best-practices">
<h3>💡 Рекомендации:</h3>
<ul>
  <li><strong>✅ Используй:</strong> <code>getSelectedValuesList()</code> для получения списка, <code>getSelectedIndices()</code> для получения индексов</li>
  <li><strong>✅ Отлично:</strong> Проверять, что список не пуст, использовать в обработчике событий</li>
  <li><strong>⚠️ Избегай:</strong> Игнорировать пустой список</li>
</ul>
<p><strong>Важно:</strong> <code>isSelectionEmpty()</code> — проверить, есть ли выбор. Проверяй на пустоту перед использованием. Используй для множественных операций.</p>
</section>

<section class="result">
<h3>👀 Результат:</h3>
<p>Можно получить все выбранные элементы. При множественном выборе можно обработать все выбранные элементы.</p>
</section>')
ON CONFLICT (documentation_id, language_code) DO NOTHING;

INSERT INTO task_translation (task_id, language_code, title, question)
VALUES (100, 'ru', 'Множественный выбор: получи все выбранные элементы',
        'Создай список с MULTIPLE_INTERVAL_SELECTION, получи все выбранные элементы через getSelectedValuesList() и выведи их')
ON CONFLICT (task_id, language_code) DO NOTHING;

